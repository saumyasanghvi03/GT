version: '3.8'

services:
  pitch-assistant:
    build: .
    ports:
      - "8501:8501"
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - STREAMLIT_SERVER_PORT=8501
    restart: always
